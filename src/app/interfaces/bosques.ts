import * as introJs from "intro.js";

export interface Bosque {
    id: string;
    nombre: string;
    zona: string;
    img_url: string;
    coords: string;
    descripcion: string;
    direccion: string;
    precio: string;
    likes: number;
}